---
phase: 05-packaging-release
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - ranger-pinot-plugin-it/pom.xml
  - ranger-pinot-plugin-it/docker/docker-compose.yml
  - ranger-pinot-plugin-it/docker/pinot/Dockerfile.plugin
  - ranger-pinot-plugin-it/src/test/java/org/apache/ranger/authorization/pinot/it/RangerRestClient.java
  - ranger-pinot-plugin-it/src/test/java/org/apache/ranger/authorization/pinot/it/PinotRangerIT.java
  - ranger-pinot-plugin-it/src/test/resources/fixtures/orders-schema.json
  - ranger-pinot-plugin-it/src/test/resources/fixtures/orders-table.json
  - ranger-pinot-plugin-it/src/test/resources/fixtures/orders.csv
  - .github/workflows/ci.yml
  - .planning/ROADMAP.md
autonomous: true
requirements: [CI-04, COMPAT-01, COMPAT-02]
must_haves:
  truths:
    - "mvn clean verify (without -Pintegration) stays green, fast, and Docker-free — ITs only run under -Pintegration"
    - "The real 05-01 distro tarball, installed by the real enable-pinot-plugin.sh into stock apachepinot/pinot images, enforces allow and deny policies authored in a real Ranger Admin (CI-04)"
    - "Every access decision emits an auditable event (log4j audit JSON in container logs, Solr as fallback channel)"
    - "A Ranger row-filter policy changes broker query results against the live cluster (Phase 3 deferred verification)"
    - "An unauthorized controller REST call is rejected with 403 and an authorized one succeeds (Phase 4 deferred verification)"
    - "A policy edit in Ranger Admin changes enforcement within the poll interval with no restart (BROKER-05 live poll)"
    - "The identical tarball artifact passes the same IT suite on both apachepinot/pinot:1.4.0 and apachepinot/pinot:1.5.1 (COMPAT-01)"
    - "CI runs the integration job as a matrix over pinot [1.4.0, 1.5.1], separate from the existing build job (COMPAT-02)"
  artifacts:
    - path: "ranger-pinot-plugin-it/pom.xml"
      provides: "failsafe-gated IT module (skipITs=true default, -Pintegration enables)"
      contains: "maven-failsafe-plugin"
    - path: "ranger-pinot-plugin-it/docker/docker-compose.yml"
      provides: "full Ranger + Pinot stack on one flat rangernw network"
      contains: "ranger-admin"
    - path: "ranger-pinot-plugin-it/docker/pinot/Dockerfile.plugin"
      provides: "derived Pinot image with real distro tarball + enable script"
      contains: "enable-pinot-plugin.sh"
    - path: "ranger-pinot-plugin-it/src/test/java/org/apache/ranger/authorization/pinot/it/PinotRangerIT.java"
      provides: "E2E allow/deny/audit/row-filter/403/live-poll tests"
      min_lines: 150
    - path: ".github/workflows/ci.yml"
      provides: "integration matrix job"
      contains: "matrix"
  key_links:
    - from: "ranger-pinot-plugin-it/pom.xml (exec-maven-plugin, -Pintegration)"
      to: "ranger-pinot-plugin-it/docker/docker-compose.yml"
      via: "docker compose up -d --build --wait / down -v around failsafe phases"
      pattern: "pre-integration-test"
    - from: "docker/pinot/Dockerfile.plugin"
      to: "ranger-admin.rangernw:6080"
      via: "POLICY_MGR_URL=http://ranger-admin.rangernw:6080 in install.properties appended before enable-pinot-plugin.sh runs"
      pattern: "POLICY_MGR_URL=http://ranger-admin"
    - from: "PinotRangerIT.java (RangerRestClient)"
      to: "http://localhost:6080/service/public/v2/api/*"
      via: "Basic auth admin:rangerR0cks! — servicedef/service/policy provisioning from repo JSON"
      pattern: "service/public/v2/api"
    - from: "PinotRangerIT.java"
      to: "http://localhost:8099/query/sql"
      via: "POST broker SQL for allow/deny/row-filter assertions"
      pattern: "query/sql"
---

<objective>
Build the docker-compose integration harness and failsafe-gated IT module that proves the shipped distro tarball enforces Ranger policies end-to-end (allow/deny/audit/row-filter/403/live-poll) against a real Ranger Admin 2.8.0 + real Pinot cluster — running the identical artifact on both Pinot 1.4.0 and 1.5.1 in a CI matrix.

Purpose: Closes CI-04, COMPAT-01, COMPAT-02 — the last open requirements — and retires every deferred live verification
from Phases 1-4 (FOUND-03/04, BROKER-05 live poll, Phase 4 live 403s, Phase 3 live row-filter).

Output: New `ranger-pinot-plugin-it` Maven module (compose stack, Dockerfile.plugin, PinotRangerIT), parent pom module
entry, GHA integration matrix job.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packaging-release/05-RESEARCH.md

<read_first>
Before writing any harness file, read:

- ranger-pinot-plugin-distro/scripts/enable-pinot-plugin.sh (install.properties keys the Dockerfile must append; how
  jars/conf land in $PINOT_HOME)
- ranger-pinot-plugin-distro/scripts/install.properties (XAAUDIT.* key names)
- ranger-pinot-plugin-distro/conf/ranger-pinot-security-changes.cfg and ranger-pinot-audit-changes.cfg (exact rendered
  config keys — note pollIntervalMs is the LITERAL 30000, not a placeholder)
- ranger-pinot-plugin/src/main/resources/service-defs/ranger-servicedef-pinot.json (what POSTs to Ranger Admin; config
  keys for the service like pinot.controller.url)
- ranger-pinot-plugin-distro/src/main/assembly/pinot-plugin.xml (tarball base dir name
  ranger-1.0.0-SNAPSHOT-pinot-plugin — glob for it, do not hardcode the version)
- pom.xml and .github/workflows/ci.yml (what exists; the build job must stay untouched)
  </read_first>

<interfaces>
From the 05-01 distro (verified in-repo):
- enable script reads install.properties keys: POLICY_MGR_URL, REPOSITORY_NAME, COMPONENT_INSTALL_DIR_NAME (absolute → PINOT_HOME), POLICY_CACHE_FILE_PATH, XAAUDIT.LOG4J.ENABLE, XAAUDIT.SOLR.ENABLE/URL/FILE_SPOOL_DIR
- enable script sets: pinot.broker.access.control.class / pinot.broker.enable.row.column.level.auth in $PINOT_HOME/conf/pinot-broker.conf; controller.admin.access.control.factory.class in pinot-controller.conf
- rendered ranger-pinot-security.xml keys: ranger.plugin.pinot.service.name=%REPOSITORY_NAME%, ranger.plugin.pinot.policy.rest.url=%POLICY_MGR_URL%, ranger.plugin.pinot.policy.pollIntervalMs=30000 (literal), ranger.plugin.pinot.policy.cache.dir=%POLICY_CACHE_FILE_PATH%
- tarball layout: base dir `ranger-${version}-pinot-plugin/` containing lib/ (shim + classloader), lib/ranger-pinot-plugin-impl/ (43 impl jars), enable-pinot-plugin.sh, install.properties, install/conf.templates/enable/
- service-def accessType used by policies: "query" on resource "table" (plus ~50 controller Actions accessTypes); row-filter via rowFilterPolicyItems
</interfaces>

Known carried concern (NOT a plan gap): broker-side identity is derived from RequesterIdentity.getClientIp () — per-user
broker tests are impossible upstream. Per-user assertions run on the controller path (Basic auth); broker path is tested
at table level (allow vs deny table) and via row-filter, which is identity-independent in effect. Document this
limitation in the test class javadoc.
</context>

<tasks>

<task type="auto">
  <name>Task 1: IT module skeleton + docker-compose harness</name>
  <files>pom.xml, ranger-pinot-plugin-it/pom.xml, ranger-pinot-plugin-it/docker/docker-compose.yml, ranger-pinot-plugin-it/docker/pinot/Dockerfile.plugin</files>
  <action>
    <read_first>ranger-pinot-plugin-distro/scripts/enable-pinot-plugin.sh, ranger-pinot-plugin-distro/scripts/install.properties, .planning/phases/05-packaging-release/05-RESEARCH.md Patterns 1-2</read_first>

    1. Add `<module>ranger-pinot-plugin-it</module>` to the root pom.xml `<modules>`. Also extend the root pom's apache-rat-plugin `<excludes>` (match the existing `<exclude>` ant-pattern style) with:
       - `<exclude>ranger-pinot-plugin-it/docker/**</exclude>` (docker-compose.yml + Dockerfile.plugin are headerless — RAT would fail the build)
       - `<exclude>ranger-pinot-plugin-it/src/test/resources/**/*.json</exclude>` and `<exclude>ranger-pinot-plugin-it/src/test/resources/**/*.csv</exclude>` (fixture data files are headerless)
       Note: checkstyle runs with `includeTestSourceDirectory=true` already, so the IT Java sources must be checkstyle-clean (license header, naming, imports) — no suppression needed, just write clean code. No other root pom changes.

    2. Create `ranger-pinot-plugin-it/pom.xml`:
       - packaging jar; parent = org.apache.ranger:ranger-pinot-plugin-parent
       - properties: `<skipITs>true</skipITs>`, `<it.pinot.version>1.4.0</it.pinot.version>`
       - dependencies: junit-jupiter (test), plus a normal dependency on org.apache.ranger:ranger-pinot-plugin-distro:${project.version} with `<type>tar.gz</type>` (the attached assembly artifact) — used ONLY so the reactor guarantees the tarball exists before this module builds; do NOT depend on impl jars (CLASSLOAD-01: the harness installs via the tarball, never via classpath)
       - maven-dependency-plugin: `copy` execution bound to `generate-test-resources`, copying `org.apache.ranger:ranger-pinot-plugin-distro:${project.version}:tar.gz` into `${project.build.directory}` so the Docker build context sees `target/ranger-*-pinot-plugin.tar.gz`
       - maven-failsafe-plugin 3.2.5 (version-match surefire in root pom): executions `integration-test`+`verify`, `<skipITs>${skipITs}</skipITs>`, systemPropertyVariables: `ranger.baseUrl=http://localhost:6080`, `pinot.controller.baseUrl=http://localhost:9000`, `broker.baseUrl=http://localhost:8099`, `it.pinot.version=${it.pinot.version}`, `it.module.dir=${project.basedir}`, `it.compose.project=ranger-pinot-it`
       - `integration` profile: sets `<skipITs>false</skipITs>` and adds exec-maven-plugin executions:
         - id `compose-up`, phase `pre-integration-test`, goal exec, commandline: `docker compose -f docker/docker-compose.yml -p ranger-pinot-it up -d --build --wait`, workingDirectory `${project.basedir}`, environmentVariables `PINOT_VERSION=${it.pinot.version}`
         - id `compose-down`, phase `post-integration-test`, goal exec, commandline: `docker compose -f docker/docker-compose.yml -p ranger-pinot-it down -v --remove-orphans`
         - Add `<onFailure>run</onFailure>` behavior to compose-down by also binding it to a second execution or verifying exec-maven-plugin runs post-integration-test on test failure (it does — post phases run even when integration-test fails, as long as the verify goal fails the build afterwards)
       - IMPORTANT: failsafe and exec-maven-plugin live ONLY in the `integration` profile, so `mvn clean verify` builds the module (compiling nothing — no main sources) in milliseconds without Docker

    3. Create `ranger-pinot-plugin-it/docker/docker-compose.yml` — ONE file, ONE flat network named `rangernw` (per research: single network avoids all DNS questions between the Ranger and Pinot stacks):
       - networks: `rangernw: { name: rangernw }`
       - service `ranger-db`: image apache/ranger-db:2.8.0, hostname ranger-db.rangernw, env POSTGRES_PASSWORD=rangerR0cks!, RANGER_DB_USER=rangeradmin, RANGER_DB_PASSWORD=rangerR0cks!, healthcheck `su -c 'pg_isready -q' postgres` (interval 10s, retries 30), no host ports
       - service `ranger-solr`: image apache/ranger-solr:2.8.0, hostname ranger-solr.rangernw, command `solr-precreate ranger_audits /opt/solr/server/solr/configsets/ranger_audits/`, host port 8983:8983 (audit fallback channel)
       - service `ranger-admin`: image apache/ranger:2.8.0, hostname ranger-admin.rangernw, env same three POSTGRES/DB vars as ranger-db, host port 6080:6080, depends_on ranger-db healthy + ranger-solr started. Do NOT add a curl-based healthcheck to ranger-admin (image may lack curl) — readiness is handled by the test's poll loop (Ranger first boot is 2-5 min; Pitfall 2: poll, never sleep)
       - service `pinot-zookeeper`: image zookeeper:3.9.5, healthcheck `zkServer.sh status`, no host port
       - services `pinot-controller` / `pinot-broker` / `pinot-server`: each `build: { context: .., dockerfile: docker/pinot/Dockerfile.plugin, args: { PINOT_VERSION: "${PINOT_VERSION}" } }` (context `..` is the module root — contains target/ with the tarball); commands `StartController -zkAddress pinot-zookeeper:2181` / `StartBroker -zkAddress pinot-zookeeper:2181` / `StartServer -zkAddress pinot-zookeeper:2181`; env JAVA_OPTS=`-Xms512M -Xmx1G` per component (docs' 4-16G defaults OOM a 16GB CI runner — Pitfall 3); controller host port 9000:9000, broker 8099:8099, server 8098:8098; controller and broker depend_on pinot-zookeeper healthy AND ranger-admin service_started (plugin polls Ranger from boot); all services on network rangernw
       - service `pinot-controller` additionally mounts `./../src/test/resources/fixtures:/fixtures:ro` (schema/table/CSV used for segment generation)

    4. Create `ranger-pinot-plugin-it/docker/pinot/Dockerfile.plugin`:
       ```dockerfile
       ARG PINOT_VERSION=1.4.0
       FROM apachepinot/pinot:${PINOT_VERSION}
       COPY target/ranger-*-pinot-plugin.tar.gz /tmp/plugin.tar.gz
       RUN mkdir -p /opt/ranger && \
           tar -xzf /tmp/plugin.tar.gz -C /opt/ranger --strip-components=1 && \
           printf 'COMPONENT_INSTALL_DIR_NAME=/opt/pinot\nPOLICY_MGR_URL=http://ranger-admin.rangernw:6080\nREPOSITORY_NAME=pinotdev\nPOLICY_CACHE_FILE_PATH=/tmp/ranger-policycache\nSSL_KEYSTORE_FILE_PATH=/dev/null\nXAAUDIT.LOG4J.ENABLE=true\nXAAUDIT.SOLR.ENABLE=true\nXAAUDIT.SOLR.URL=http://ranger-solr.rangernw:8983/solr/ranger_audits\nXAAUDIT.SOLR.FILE_SPOOL_DIR=/tmp/audit-spool\n' \
             >> /opt/ranger/install.properties && \
           sed -i 's/^ranger.plugin.pinot.policy.pollIntervalMs.*/ranger.plugin.pinot.policy.pollIntervalMs\\t5000/' /opt/ranger/install/conf.templates/enable/ranger-pinot-security-changes.cfg || true && \
           /opt/ranger/enable-pinot-plugin.sh
       ```
       Notes: use the plain `apachepinot/pinot` tags (JDK 21-era) — plugin class files are `--release 17`, they load on JDK 21 but NOT on a JDK 11 image variant (Pitfall 6; never use `-java-11` tags). The pollIntervalMs sed lowers the poll to 5s for the BROKER-05 live-poll test (the changes.cfg value is the literal 30000 — verified in the cfg file; the `|| true` covers the tab-vs-spaces sed mismatch; if the sed does not match, adjust to the exact line format from the cfg). Verify `$PINOT_HOME` in the stock image is `/opt/pinot` (`docker run --rm apachepinot/pinot:1.4.0 ls /opt/pinot`); if not, set COMPONENT_INSTALL_DIR_NAME to the actual value.

       The `SSL_KEYSTORE_FILE_PATH=/dev/null` append is required: install.properties ships the default `/etc/pinot/conf/ranger-plugin-keystore.jks`, which the enable script would render into `ranger-policymgr-ssl.xml` — a config pointing at a nonexistent file. Appending `/dev/null` overrides the default (the enable script's `getInstallProperty` takes the LAST matching line), matching what the script does for an empty value. Sanity-assert the rendered `ranger-policymgr-ssl.xml` contains `/dev/null`, not the jks path.

    5. Wave-0 sanity gate (run before committing): `mvn -pl ranger-pinot-plugin-distro -am clean verify` produces the tarball; `cd ranger-pinot-plugin-it && mvn org.apache.maven.plugins:maven-dependency-plugin:3.7.1:copy@... ` (or plain `mvn -Pintegration generate-test-resources`), then `PINOT_VERSION=1.4.0 docker compose -f docker/docker-compose.yml -p sanity up -d --build --wait` → poll `curl -fsS http://localhost:6080` until 200 (up to 5 min, first boot) → ASSERT the broker actually booted and fetched policies over plain http: `docker compose logs pinot-broker` shows the Ranger factory class loading AND a successful policy download (no SSL/keystore errors, no policy-fetch exceptions — e.g. `GET http://ranger-admin.rangernw:6080/.../policies/download` succeeding or the policy-cache file appearing in /tmp/ranger-policycache). Also verify the broker REST endpoint is up (`curl -fsS http://localhost:8099/health` returns 200). Then `docker compose down -v`. If the ranger-admin stack fails with JPA/hostname errors, re-check against RESEARCH Pattern 1 exactly (the standalone-ranger failure mode — Pitfall 1).

  </action>
  <verify>
    <automated>mvn -B clean verify -pl ranger-pinot-plugin-it -am --no-transfer-progress && docker compose -f ranger-pinot-plugin-it/docker/docker-compose.yml config -q && (cd ranger-pinot-plugin-it && PINOT_VERSION=1.4.0 docker build -f docker/pinot/Dockerfile.plugin --build-arg PINOT_VERSION=1.4.0 -t ranger-pinot-it:sanity .)</automated>
  </verify>
  <done>
    - `mvn -B clean verify -pl ranger-pinot-plugin-it -am` (from repo root, no profile) is green: module + distro build through the reactor, no Docker, no IT execution (skipITs=true, failsafe/exec-maven-plugin not even configured outside the `integration` profile)
    - `docker compose config -q` validates the compose file
    - The derived image builds: tarball extracted, enable-pinot-plugin.sh runs successfully, `/opt/pinot/conf/ranger-pinot-security.xml` rendered with `pinotdev` + `http://ranger-admin.rangernw:6080`, `pinot-broker.conf` contains `pinot.broker.access.control.class`
    - `-Pintegration` profile contains exactly the failsafe + exec-maven-plugin wiring, nothing else
  </done>
</task>

<task type="auto">
  <name>Task 2: PinotRangerIT — E2E allow/deny/audit/row-filter/403/live-poll + FOUND-03/04</name>
  <files>ranger-pinot-plugin-it/src/test/java/org/apache/ranger/authorization/pinot/it/RangerRestClient.java, ranger-pinot-plugin-it/src/test/java/org/apache/ranger/authorization/pinot/it/PinotRangerIT.java, ranger-pinot-plugin-it/src/test/resources/fixtures/orders-schema.json, ranger-pinot-plugin-it/src/test/resources/fixtures/orders-table.json, ranger-pinot-plugin-it/src/test/resources/fixtures/orders.csv</files>
  <action>
    <read_first>ranger-pinot-plugin/src/main/resources/service-defs/ranger-servicedef-pinot.json (service config key for the controller URL — use its exact key name in the service configs map), .planning/phases/05-packaging-release/05-RESEARCH.md Code Examples section (REST payload shapes)</read_first>

    Package: `org.apache.ranger.authorization.pinot.it`. All HTTP via JDK 17 `java.net.http.HttpClient` — NO new dependencies (no testcontainers, no okhttp, no awaitility: hand-roll poll loops with deadline + Thread.sleep backoff).

    1. `RangerRestClient.java` — thin REST helper:
       - Basic auth `admin:rangerR0cks!` against `${ranger.baseUrl}` (http://localhost:6080)
       - `awaitRangerReady()`: poll `GET /` with 5-minute deadline (first boot takes 2-5 min)
       - `deleteServiceDefIfExists()`: `DELETE /service/public/v2/api/servicedef/name/pinot?forceDelete=true`; ignore 404 (idempotent re-runs; first-boot `create-ranger-services.py` does not create pinot services, but the delete makes re-runs against a warm stack safe)
       - `postServiceDef()`: POST the repo's own `service-defs/ranger-servicedef-pinot.json` (read from the repo checkout via `it.module.dir` system property — the file is NOT copied into the module; single source of truth)
       - `postService()`: POST /service/public/v2/api/service with `{"name":"pinotdev","type":"pinot","configs":{<controller-url key from servicedef>:"http://pinot-controller:9000","username":"admin","password":"admin"}}` (configs keys must match the service-def's `configs[]` entries — read the JSON)
       - `postPolicy/deletePolicy/updatePolicy` against /service/public/v2/api/policy (POST, DELETE /policy/{id}, PUT /policy/{id})
       - `createTable(schemaJson, tableJson)`, `queryBroker(sql)` → POST `${broker.baseUrl}/query/sql` `{"sql": ...}`, `controllerGet(path)` / `controllerPost(path, body, basicAuthUser, basicAuthPass)` for the 403 tests
       - `composeLogs(service)`: ProcessBuilder `docker compose -f <it.module.dir>/docker/docker-compose.yml -p ranger-pinot-it logs <service>` — used for audit-log grep
       - `pinotAdmin(args)`: ProcessBuilder `docker compose ... exec -T pinot-controller /opt/pinot/bin/pinot-admin.sh <args>` — used for segment generation

    2. Fixtures (`src/test/resources/fixtures/`):
       - `orders-schema.json`: dimension columns `orderId`(STRING), `region`(STRING), `amount`(INT); `orders-table.json`: OFFLINE table `orders` on pinot-server; `orders.csv`: 4 rows — 2 with `region=west`, 2 with `region=east` (schema/table JSON shapes per Pinot REST API; verify field names against the pinot-controller version in-repo via its provided dependency sources or docs)

    3. `PinotRangerIT.java` — JUnit 5, class name MUST end in `IT` (failsafe convention; surefire never picks it up — Pitfall 8). Guard first: `Assumptions.assumeTrue(docker + compose available)` (`docker info` via ProcessBuilder) so `-Pintegration` without Docker skips instead of hanging. `@TestMethodOrder(OrderAnnotation)` — one ordered E2E story against the single shared stack (compose up/down is per-build via exec-maven-plugin, not per-test):
       - **@BeforeAll**: awaitRangerReady → deleteServiceDefIfExists → postServiceDef → postService → createTable → generate+upload segment (pinotAdmin AddSegment inside the controller container using /fixtures/orders.csv, then POST the segment to the controller; if AddSegment-in-container proves awkward, generate the segment via the controller's own upload API — adapt, but end state must be: the `orders` table exists with 4 rows loaded. Do NOT seed any policy here — the allow test's discovery step needs a policy-free (deny-by-default) first query; segment load can be confirmed via the controller REST (`GET /tables/orders` metadata / segments count) or via the first broker query after the allow policy is created)
       - **test FOUND-03/04 (deferred Phase 1 verifications)**: (a) FOUND-03 — after postService, Ranger Admin invoked `RangerServicePinot.validateConfig()` against the live controller; assert the service was created and retrievable via `GET /service/public/v2/api/service/name/pinotdev` (a failed validateConfig would reject/reveal in the create response). Also attempt the explicit test-connection surface `PUT/POST /service/public/v2/api/service/name/pinotdev/validateConfig` or `/testConfig` (MEDIUM confidence — research open question 3): if the endpoint returns 404/405, fall back to the create-response assertion and note the fallback in the summary; do NOT fail the test on a missing endpoint. (b) FOUND-04 — attempt `GET /service/public/v2/api/service/name/pinotdev/resources?table=ord` (lookupResource surface; same MEDIUM confidence): if it 404s, fall back to asserting the service-def's lookupResource implementation is exercised via the Admin UI REST path is unavailable, and instead assert indirectly — a policy referencing table `orders` evaluates correctly (the resource-matching path is proven by every allow/deny test below); document the fallback.
       - **test allow (CI-04)**: broker-path identity is NOT a username — the broker `deriveUser` returns `RequesterIdentity.getClientIp()` (docker gateway IP, e.g. `172.x.0.1`), so a policy for user `pinot-admin-test` will NOT authorize broker queries. Determine the derived principal at RUNTIME (do NOT hardcode or guess): (a) with NO policy yet in place, run one discovery query `SELECT count(*) FROM orders` via the broker → denied (Ranger deny-by-default); (b) grep the deny audit event from `composeLogs("pinot-broker")` (or Solr `http://localhost:8983/solr/ranger_audits/select?q=service:pinotdev`) and parse the `usr` field → `derivedBrokerUser` (assert it is non-empty and IP-shaped — it IS the policy principal for the broker path); (c) create the allow policy with that exact user:
         ```json
         {
           "service": "pinotdev",
           "name": "it-allow-query-orders",
           "resources": { "table": { "values": ["orders"] } },
           "policyItems": [ { "accesses": [ { "type": "query", "isAllowed": true } ], "users": ["<derivedBrokerUser>"] } ]
         }
         ```
         → wait for poll (≤5s + margin) → `SELECT count(*) FROM orders` → 200 with numResults 4. (Do NOT use `users:["*"]` — Ranger wildcard-user support in policyItems is not verified for 2.8.0; runtime discovery needs no such assumption.)
       - **test deny (CI-04)**: same derived principal, a deny policy: `"denyPolicyItems": [ { "accesses": [ { "type": "query", "isAllowed": true } ], "users": ["<derivedBrokerUser>"] } ]` on table `orders` (deny wins over allow). Assert `SELECT count(*) FROM orders` is REJECTED by the broker (Pinot returns QueryException / error response with code 403 or a broker error — assert non-200 or error body, exact shape verified at write time). Delete the deny policy, keep the allow policy (restored for subsequent tests).
       - **test audit (CI-04, BROKER-04 live)**: after one allowed + one denied broker query, grep `composeLogs("pinot-broker")` (and pinot-controller for the 403 test below) for the Ranger audit JSON: contains `"service":"pinotdev"` and `"accessType":"query"` and both an allow and a deny result. Ranger Log4jAuditProvider writes to a log4j logger that defaults to the container console — verify the exact JSON field names against ranger-audit-core's `AuditProviderBase`/Log4jAuditProvider sources (available as the distro's impl jars) at write time. Fallback channel if log4j audit is not visible in container logs: query Solr `http://localhost:8983/solr/ranger_audits/select?q=service:pinotdev` (Solr audit is enabled in install.properties); implement log4j-grep primary, Solr fallback, assert on at least one channel.
       - **test row filter (MASK-01 live, Phase 3 deferred)**: row-filter policy on table orders: rowFilterPolicyItems [{rowFilterInfo filterExpr `region = 'west'`, access query allowed}] for the SAME `derivedBrokerUser` principal discovered in the allow test. Assert broker SQL `SELECT count(*) FROM orders` returns 2 (west rows only). Delete the row-filter policy afterwards.
       - **test controller 403 (Phase 4 deferred)**: with allow-query policy only (no CREATE access), POST to controller `/tables` with Basic auth `pinot-admin-test:pw` → expect 403; then grant a policy with the CreateTable accessType to user `pinot-admin-test` (use the exact accessType name from the service-def — read it from ranger-servicedef-pinot.json) → retry table create (different table name) → expect 201/200. This is the per-user path (Basic auth works end-to-end on the controller per RangerPinotAccessControl.extractUser).
       - **test live poll (BROKER-05 deferred)**: with allow policy active and query returning 4, DELETE the allow policy, wait pollIntervalMs (5s) + margin (poll the broker query with deadline 30s, sleep 1s between attempts), assert the query is now denied without any restart; then re-create the allow policy and assert the query succeeds again within the same 30s deadline — no container restart anywhere in this test.
    4. Keep each test to plain JUnit asserts; no assertion libraries (only junit-jupiter is on the test classpath).
    5. Split permission: if context strains while writing PinotRangerIT, you MAY split it into two `*IT` classes without re-planning — `PinotRangerCoreIT` (allow/deny/audit, i.e. CI-04 essentials) and `PinotRangerExtendedIT` (row-filter, controller 403, live poll, FOUND-03/04). Both run against the single shared compose stack; keep the ordered-story structure within each class (CoreIT runs first via `@Order` on the class or failsafe's default ordering — set `@TestMethodOrder` per class and ensure the deny-discovery/allow sequence lands in CoreIT).

  </action>
  <verify>
    <automated>mvn -B clean verify -Pintegration -Dit.pinot.version=1.4.0 --no-transfer-progress</automated>
  </verify>
  <done>
    - `mvn clean verify` (root) unchanged: green, no Docker (ITs skipped by failsafe naming + skipITs)
    - `mvn -B clean verify -Pintegration -Dit.pinot.version=1.4.0` (repo root, full reactor — distro tarball built through the reactor, no local-repo dependency) passes all tests: allow, deny, audit, row-filter, controller 403, live poll, FOUND-03 (+ FOUND-04 with documented fallback if its REST surface doesn't exist)
    - Same command with `-Dit.pinot.version=1.5.1` passes the identical suite against the 1.5.1 image — same tarball artifact, no rebuild of plugin modules (COMPAT-01)
    - Test class javadoc documents the broker identity limitation (client-IP stand-in, upstream RequesterIdentity has no user)
  </done>
</task>

<task type="auto">
  <name>Task 3: GitHub Actions integration matrix job</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    <read_first>.github/workflows/ci.yml (leave the existing build job byte-for-byte untouched), .github/workflows/release.yml (runner/setup-java conventions to mirror)</read_first>

    Append a second job `integration` to .github/workflows/ci.yml after `build`:
    ```yaml
    integration:
      needs: build
      runs-on: ubuntu-22.04
      timeout-minutes: 30
      strategy:
        fail-fast: false
        matrix:
          pinot: ["1.4.0", "1.5.1"]
      steps:
        - name: Checkout
          uses: actions/checkout@v4
        - name: Set up JDK 17 (Temurin)
          uses: actions/setup-java@v5
          with: { distribution: temurin, java-version: '17' }
        - name: Cache Maven dependencies
          uses: actions/cache@v4
          with:
            path: ~/.m2/repository
            key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
            restore-keys: ${{ runner.os }}-maven-
        - name: Run integration tests (Pinot ${{ matrix.pinot }})
          run: mvn -B clean verify -Pintegration -Dit.pinot.version=${{ matrix.pinot }} --no-transfer-progress -V
    ```
    Notes: docker + compose v2 are preinstalled on ubuntu runners; the job builds the tarball fresh (single reactor build, artifact compiled once, then the IT installs that tarball into the matrix image — COMPAT-01/02 semantics). Do NOT parallelize with `-T` (the IT stack pins host ports 6080/9000/8099; a single stack per job). Keep timeout-minutes at 30 (ranger first boot 2-5 min per Pitfall 2). The build job and release.yml stay exactly as they are.

  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" && grep -q "matrix" .github/workflows/ci.yml && grep -q "needs: build" .github/workflows/ci.yml</automated>
  </verify>
  <done>
    - ci.yml has both jobs: build (unchanged) + integration (matrix pinot [1.4.0, 1.5.1], needs build, fail-fast disabled)
    - YAML parses; matrix values exactly match the verified Docker Hub tags
    - No changes to release.yml or the build job steps
  </done>
</task>

</tasks>

<verification>
Phase-level checks (also cover the deferred verifications from Phases 1-4):
1. `mvn clean verify` — fast path unchanged, no Docker, all existing unit tests still green
2. `mvn clean verify -Pintegration -Dit.pinot.version=1.4.0` — full E2E suite green on Pinot 1.4.0
3. `mvn clean verify -Pintegration -Dit.pinot.version=1.5.1` — same suite green on Pinot 1.5.1, identical tarball artifact (COMPAT-01)
4. `docker compose -f ranger-pinot-plugin-it/docker/docker-compose.yml config -q` — compose valid
5. ci.yml valid YAML with build + integration matrix jobs (COMPAT-02)
</verification>

<success_criteria>

- CI-04: real Ranger Admin 2.8.0 (postgres + solr sidecars) + real Pinot cluster (ZK/controller/broker/server) via
  docker-compose; ITs verify allow, deny, audit, and row-filter end-to-end using the real distro tarball installed by
  the real enable script
- COMPAT-01: one build of the distro tarball, installed unmodified into both apachepinot/pinot:1.4.0 and :1.5.1 derived
  images, passing the identical test suite
- COMPAT-02: GitHub Actions integration job runs the suite as a matrix over both Pinot versions, independent of the
  existing build job
- Deferred verifications retired where research confidence supports: FOUND-03 (validateConfig live), BROKER-05 (live
  poll), Phase 4 (live 403), Phase 3 (live row-filter); FOUND-04 attempted with a documented fallback assertion if the
  lookupResource REST surface is absent (MEDIUM confidence in research — never fail on a missing endpoint)
- Known limitation documented, not hidden: per-user broker tests are impossible upstream (RequesterIdentity has no
  user); per-user assertions live on the controller path
  </success_criteria>

<output>
After completion, create `.planning/phases/05-packaging-release/05-03-SUMMARY.md`
</output>
