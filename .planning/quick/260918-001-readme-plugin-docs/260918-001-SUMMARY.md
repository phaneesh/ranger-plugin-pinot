# Quick Task 260918-001: Summary

Created `README.md` at repo root with full plugin documentation:

- Overview + features (broker/controller authz, RLS, tag policies, audit, fail-closed, policy cache)
- Requirements (Pinot 1.4.x/1.5.x, Ranger 2.8.x, JDK 17/Maven build)
- Build instructions + tag-triggered release flow
- Installation: Ranger Admin service-def load + service creation, install.properties table, enable-pinot-plugin.sh behavior, restart; disable script
- Configuration: ranger-pinot-security.xml property table, audit config, classpath note
- Resource model (table/cluster, 133 access types) + policy examples
- Identity derivation caveat
- Integration tests (-Pintegration, docker-compose stack, CI matrix)
- Project layout + license

All content verified against the actual repo (distro scripts/conf, service-def JSON, workflows, IT module). markdownlint clean (no repo config; only default line-length advisories).
