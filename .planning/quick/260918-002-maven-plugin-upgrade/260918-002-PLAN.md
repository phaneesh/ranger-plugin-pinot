---
id: 260918-002
description: Upgrade all maven plugins to the latest available version
tasks: 1
---

# Quick Task 260918-002: Upgrade Maven Plugins

## Findings (mvn versions:display-plugin-updates, Maven 3.9.16)

All plugins checked against latest versions. CI builds on Java 17 (Maven 3.9.x), local JDK 25.

| Plugin                           | Current | Latest (3.6.3-compatible) | Action                                     |
|----------------------------------|---------|---------------------------|--------------------------------------------|
| maven-compiler-plugin            | 3.13.0  | 3.16.0                    | bump                                       |
| maven-surefire-plugin            | 3.2.5   | 3.6.0                     | bump                                       |
| maven-failsafe-plugin (it)       | 3.2.5   | 3.6.0                     | bump                                       |
| maven-checkstyle-plugin          | 3.1.0   | 3.6.0                     | bump                                       |
| apache-rat-plugin                | 0.16.1  | 0.18                      | bump (needs Maven 3.9 — CI uses 3.9.x, OK) |
| spotbugs-maven-plugin            | 4.7.3.5 | 4.10.4.1                  | bump                                       |
| maven-antrun-plugin (distro, it) | 3.1.0   | 3.2.0                     | bump                                       |
| maven-assembly-plugin (distro)   | 3.7.1   | 3.8.0                     | bump                                       |
| exec-maven-plugin (it)           | 3.6.3   | 3.6.4                     | bump                                       |

Checkstyle engine (8.29 → 8.45.1) is a dependency, not a plugin — bumped minor-only to keep
checkstyle.xml config compatible. Latest major 14.x risks config breakage; out of scope.

## Constraints

- maven-compiler-plugin 4.x / surefire 4.x are betas or Maven-4-only → stay 3.x.
- Parent pom uses version properties; module poms (distro, it) hardcode plugin versions inline.
- `.planning/` tracked in repo — artifacts committed via pi-gsd-tools.

## Task 1: Bump versions

- files: pom.xml, ranger-pinot-plugin-distro/pom.xml, ranger-pinot-plugin-it/pom.xml
- action: set plugin version properties/inline versions per table above; bump checkstyle engine to 8.45.1
- verify: `mvn -N versions:display-plugin-updates` per module reports no Maven-3.6.3-compatible updates remaining;
  `mvn -q verify -DskipTests` passes (rat/checkstyle/spotbugs still green)
- done: build green with all plugins on latest compatible versions
